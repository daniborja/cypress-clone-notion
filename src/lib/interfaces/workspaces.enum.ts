export enum WorkspacesPermissions {
  private = 'private',
  shared = 'shared',
}

export enum WPListType {
  folder = 'folder',
  file = 'file',
  workspace = 'workspace',
}

export enum WPDirType {
  folder = 'folder',
  file = 'file',
  workspace = 'workspace',
}
